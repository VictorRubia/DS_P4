<%= form_with(model: transfer) do |form| %>
  <% if transfer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(transfer.errors.count, "error") %> prohibited this transfer from being saved:</h2>

      <ul>
        <% transfer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount %>
  </div>

  <div class="field">
    <%= form.label :account_id %>
    <%= form.text_field :account_id %>
  </div>

  <div class="field">
    <%= form.label :account2_id %>
    <%= form.text_field :account2_id %>
  </div>

  <div class="field">
    <%= form.label :concepto %>
    <%= form.text_field :concepto %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
