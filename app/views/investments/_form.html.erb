<%= form_with(model: investment) do |form| %>
  <% if investment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(investment.errors.count, "error") %> prohibited this investment from being saved:</h2>

      <ul>
        <% investment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount %>
  </div>

  <div class="field">
    <%= form.label :meses %>
    <%= form.number_field :meses %>
  </div>

  <div class="field">
    <%= form.label :interes %>
    <%= form.text_field :interes %>
  </div>

  <div class="field">
    <%= form.label :account_id %>
    <%= form.text_field :account_id %>
  </div>

  <div class="field">
    <%= form.label :concepto %>
    <%= form.text_field :concepto %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
